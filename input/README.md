# Input Directory

Put source lecture videos here for local batch processing.

Examples:

- `lecture01.mp4`
- `lecture02.mkv`
- `week3.mov`

The batch workflow in `scripts/process_input_output.sh` scans this directory and writes generated subtitles and transcripts to `output/`.

